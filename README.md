# Simple Laravel 10 User Roles and Permissions
Learn how to develop simple Laravel 10 user roles and permissions application

> The complete tutorial step by step guide is available on my blog. [AllPHPTricks Laravel roles and permissions](https://www.allphptricks.com/simple-laravel-10-user-roles-and-permissions/)

## Blog
https://www.allphptricks.com/

## การอัปเดตล่าสุด (13 พฤศจิกายน 2025)

### 💰 ระบบการเงินและบัญชี (Finance & Accounting System)
เพิ่มระบบจัดการการเงินแบบครอบคลุมสำหรับการบริหารเงินมัดจำ CID และการชำระเงิน:

#### ฟีเจอร์ใหม่ที่เพิ่ม: 13/11/2025
- **ระบบ Finance Tab** (`resources/views/labours/create.blade.php`, `resources/views/labours/edit.blade.php`)
  - แท็บการเงินและบัญชีแยกเฉพาะ (เข้าถึงได้เฉพาะผู้ใช้ที่มีสิทธิ์ `account-update-labour`)
  - การจัดการเงินมัดจำ CID (วันที่, จำนวนเงิน, สถานะ, วิธีการชำระ)
  - การจัดการเงิน CID-P (วันที่ยื่น, จำนวนเงิน, วันที่เข้า, จำนวนเงินที่เข้า)
  - ระบบคืนเงินมัดจำ (วันที่คืน, จำนวนเงินคืน)
  - การคำนวณสรุปการเงินแบบเรียลไทม์

- **การอัปเดต Database Schema**
  ```sql
  -- เพิ่มฟิลด์การเงินในตาราง labours
  labour_cid_deposit_date         -- วันที่มัดจำ CID
  labour_cid_deposit_total        -- จำนวนเงินมัดจำ CID
  labour_cidp_date               -- วันที่ CID-P
  labour_cidp_total              -- จำนวนเงิน CID-P
  labour_cidp_in_date            -- วันที่ CID-P เข้า
  labour_cidp_in_total           -- จำนวนเงิน CID-P เข้า
  payment_type                   -- วิธีการชำระเงิน
  labour_cid_deposit_status      -- สถานะการมัดจำ
  labour_refund_deposit_date     -- วันที่คืนเงินมัดจำ
  labour_refund_deposit_total    -- จำนวนเงินคืน
  labour_cid_stand_date          -- วันที่ยื่น CID (สำหรับคำนวณการแจ้งเตือน)
  ```

- **ระบบแจ้งเตือนการเงิน** (`app/Http/Controllers/DashboardController.php`)
  - แจ้งเตือนเงินมัดจำ CID ค้างชำระ (เกิน 15 วันหลังยื่น CID)
  - การแจ้งเตือนแบบเรียลไทม์ในหน้า Dashboard
  - ส่งออกรายงานเงินค้างชำระเป็น Excel

- **อัปเดตฟอร์ม Labour**
  - เพิ่ม Finance Tab ในฟอร์มสร้างและแก้ไขคนงาน
  - การคำนวณยอดรวมแบบอัตโนมัติ (เงินมัดจำ + เงินเข้า - เงินคืน = ยอดคงเหลือ)
  - ตัวเลือกวิธีการชำระเงิน (เงินสด, โอนเงิน, เช็ค, บัตรเครดิต)
  - ระบบสีสำหรับแสดงสถานะยอดคงเหลือ (เขียว = บวก, แดง = ลบ, น้ำเงิน = เท่ากับศูนย์)

- **อัปเดต Dashboard**
  - เพิ่มการ์ดแจ้งเตือนเงินมัดจำ CID ค้างชำระ
  - หน้ารายละเอียดสำหรับเงินค้างชำระพร้อมข้อมูลคนงาน
  - ส่งออกรายงานเงินค้างชำระเป็น Excel

#### คุณสมบัติหลัก:
- **การควบคุมสิทธิ์**: แท็บการเงินแสดงเฉพาะผู้ใช้ที่มีสิทธิ์ `account-update-labour`
- **การคำนวณแบบเรียลไทม์**: คำนวณยอดรวมอัตโนมัติขณะผู้ใช้ป้อนข้อมูล
- **ระบบแจ้งเตือน**: แจ้งเตือนเงินมัดจำค้างชำระหลังยื่น CID เกิน 15 วัน
- **การรายงาน**: ส่งออกข้อมูลการเงินเป็น Excel พร้อมรายละเอียดคนงาน
- **UI ทันสมัย**: ใช้ Bootstrap และ CSS กำหนดเองสำหรับรูปลักษณ์มืออาชีพ

#### การใช้งานระบบการเงิน:
1. **การบันทึกเงินมัดจำ CID**: เลือกวันที่, ใส่จำนวนเงิน, เลือกสถานะและวิธีการชำระ
2. **การบันทึก CID-P**: บันทึกวันที่และจำนวนเงิน CID-P ทั้งขาออกและขาเข้า
3. **การคืนเงิน**: บันทึกวันที่และจำนวนเงินที่คืนให้คนงาน
4. **การติดตามแจ้งเตือน**: ระบบจะแจ้งเตือนอัตโนมัติเมื่อเงินมัดจำค้างชำระเกิน 15 วัน

## การอัปเดตก่อนหน้า (27 ตุลาคม 2025)

### 🎛️ ระบบ Dashboard ครบครัน
เพิ่มระบบแดชบอร์ดการแจ้งเตือนและสถิติแบบครอบคลุม:

#### ฟีเจอร์ใหม่ที่เพิ่ม: 27/10/2025
- **Dashboard Controller** (`app/Http/Controllers/DashboardController.php`)
  - คำนวณสถิติ (คนงานทั้งหมด, ไปทำงาน, ยกเลิก, สถานะ VISA)
  - ระบบแจ้งเตือนเอกสารหมดอายุ (แจ้งเตือน 15 วันล่วงหน้า)
  - ฟังก์ชันส่งออกรายงาน Excel
  - หน้ารายละเอียดสำหรับแต่ละหมวดสถิติ

- **การอัปเดต Database Schema**
  - เพิ่มฟิลด์ CID: `labour_cid_number`, `labour_cid_issue_date`, `labour_cid_expiry_date`
  - เพิ่มฟิลด์ Affidavit: `labour_affidavit_number`, `labour_affidavit_issue_date`, `labour_affidavit_expiry_date`
  - เพิ่มการติดตาม VISA: `labour_visa_status`, `labour_visa_submission_date`, `labour_visa_approval_date`
  - ลบฟิลด์ `labour_work_status` ที่ไม่ใช้แล้ว

- **หน้า Dashboard**
  - แดshบอร์ดทันสมัยแบบ responsive (`resources/views/dashboard/index.blade.php`)
  - การ์ดสถิติแบบ interactive พร้อม hover effects และการแสดงผลด้วย Chart.js
  - การ์ดแจ้งเตือนสำหรับเอกสารหมดอายุ (ผลโรค, พาสปอร์ต, CID, Affidavit)
  - หน้ารายละเอียดสำหรับการแจ้งเตือนและสถิติพร้อมส่งออก Excel
  - ขนาดคอนเทนเนอร์มาตรฐาน (max-width: 1200px) รูปลักษณ์เป็นมืออาชีพ

- **ระบบส่งออก Excel**
  - `NotificationExport.php` - ส่งออกการแจ้งเตือนเอกสารหมดอายุ
  - `StatisticExport.php` - ส่งออกสถิติคนงานตามหมวดหมู่
  - หัวตารางที่จัดรูปแบบและข้อมูลที่จัดรูปแบบวันที่แบบไทย

- **การอัปเดตฟอร์ม**
  - อัปเดตฟอร์มสร้าง/แก้ไขคนงานพร้อมฟิลด์ CID, Affidavit และ VISA ใหม่
  - ลบฟิลด์สถานะการทำงาน (รวมเข้ากับสถานะคนงานที่มีอยู่)

#### เส้นทาง (Routes) ที่อัปเดต:
- **การเงิน**: ฟิลด์การเงินประมวลผลอัตโนมัติใน `/labours/create` และ `/labours/edit`
- **การแจ้งเตือนการเงิน**: รวมใน `/dashboard/notification-details/unpaid_deposits`
- **ส่งออกข้อมูลการเงิน**: รวมใน `/dashboard/export-notification/unpaid_deposits`

#### เส้นทาง (Routes) ที่เพิ่มก่อนหน้า:
- `/dashboard` (แดshบอร์ดหลัก - ตั้งเป็นหน้าแรก)
- `/dashboard/notification-details/{type}` (รายการแจ้งเตือนแบบละเอียด)
- `/dashboard/statistic-details/{type}` (รายการสถิติแบบละเอียด)
- `/dashboard/export-notification/{type}` (ส่งออก Excel สำหรับการแจ้งเตือน)
- `/dashboard/export-statistic/{type}` (ส่งออก Excel สำหรับสถิติ)

#### รายละเอียดทางเทคนิค Finance System:
- **Migration**: `2025_11_12_063017_add_finance_fields_to_labours_table`
- **Model Updates**: อัปเดต `$fillable` array ใน `labourModel.php`
- **Controller**: เพิ่ม `globalsets` array ใน `labourController.php`
- **Notification Logic**: `getUnpaidDeposits()` method ใน `DashboardController.php`
- **JavaScript**: Real-time calculation functions ใน create/edit forms
- **Permissions**: ใช้ `@canany(['account-update-labour'])` directive

#### การตั้งค่าและใช้งาน:
1. **Migration**: `php artisan migrate` (ฟิลด์การเงินจะถูกเพิ่มอัตโนมัติ)
2. **Permissions**: ตรวจสอบผู้ใช้มีสิทธิ์ `account-update-labour`
3. **Global Sets**: ตั้งค่าสถานะเงินมัดจำ CID ในระบบ Global Sets
4. **Dashboard**: เข้าถึงการแจ้งเตือนการเงินผ่านหน้า Dashboard

#### ทางเทคนิค:
- การผสานรวม Chart.js สำหรับการแสดงผลข้อมูล
- Bootstrap 5 + CSS แบบกำหนดเองพร้อมเอฟเฟกต์ glassmorphism
- การออกแบบ responsive พร้อมการปรับให้เหมาะกับมือถือ
- ฟังก์ชันรีเฟรชอัตโนมัติ (ทุก 5 นาที)
- การ migrate ฐานข้อมูลอย่างปลอดภัยโดยไม่สูญเสียข้อมูล


## 📊 Database Schema - Finance Fields

### ตารางฟิลด์การเงิน (Finance Fields)
| ชื่อฟิลด์ | ประเภท | คำอธิบาย |
|----------|--------|----------|
| `labour_cid_deposit_date` | DATE | วันที่มัดจำ CID |
| `labour_cid_deposit_total` | DECIMAL(10,2) | จำนวนเงินมัดจำ CID |
| `labour_cidp_date` | DATE | วันที่ CID-P |
| `labour_cidp_total` | DECIMAL(10,2) | จำนวนเงิน CID-P |
| `labour_cidp_in_date` | DATE | วันที่ CID-P เข้า |
| `labour_cidp_in_total` | DECIMAL(10,2) | จำนวนเงิน CID-P เข้า |
| `payment_type` | VARCHAR(50) | วิธีการชำระเงิน (cash/transfer/check/credit_card) |
| `labour_cid_deposit_status` | INT | สถานะการมัดจำ (อ้างอิง Global Sets) |
| `labour_refund_deposit_date` | DATE | วันที่คืนเงินมัดจำ |
| `labour_refund_deposit_total` | DECIMAL(10,2) | จำนวนเงินคืน |
| `labour_cid_stand_date` | DATE | วันที่ยื่น CID (สำหรับคำนวณการแจ้งเตือน) |

### การคำนวณยอดคงเหลือ
```javascript
ยอดคงเหลือ = เงินมัดจำ CID + เงิน CID-P เข้า - เงินคืน
Balance = labour_cid_deposit_total + labour_cidp_in_total - labour_refund_deposit_total
```

### การแจ้งเตือนเงินค้างชำระ
- ระบบจะตรวจสอบรายการที่มี `labour_cid_stand_date` (วันที่ยื่น CID)
- เมื่อวันที่ยื่น CID ผ่านไปแล้ว 15 วัน
- และยังไม่มีการบันทึก `labour_cid_deposit_date` (ยังไม่มัดจำ)
- จะแสดงในการ์ดแจ้งเตือนบนหน้า Dashboard

## Installation 
Make sure that you have setup the environment properly. You will need minimum PHP 8.1, MySQL/MariaDB, composer and Node.js.

1. Download the project (or clone using GIT)
2. Copy `.env.example` into `.env` and configure your database credentials
3. Go to the project's root directory using terminal window/command prompt
4. Run `composer install`
5. Set the application key by running `php artisan key:generate --ansi`
6. Run migrations `php artisan migrate:fresh --seed`
7. Run `npm install`
8. Run `npm run build` to build assets
9. Start local server by executing `php artisan serve`

## 🔧 Finance System Configuration

### การตั้งค่าสิทธิ์ผู้ใช้
ระบบการเงินต้องการสิทธิ์ `account-update-labour` เพื่อเข้าถึงแท็บการเงิน:

```php
// ตรวจสอบสิทธิ์ใน Blade template
@canany(['account-update-labour'])
    <!-- Finance Tab แสดงเฉพาะผู้ใช้ที่มีสิทธิ์ -->
@endcanany
```

### การตั้งค่า Global Sets สำหรับสถานะเงินมัดจำ
1. เข้าไปที่หน้า Global Sets
2. เพิ่มค่าสถานะสำหรับ `labour_cid_deposit_status`:
   - "รอดำเนินการ"
   - "ได้รับแล้ว"
   - "ขาดส่วน"
   - "คืนเงินแล้ว"

## 📋 การใช้งานระบบการเงิน

### 1. การบันทึกเงินมัดจำ CID
- เลือกวันที่มัดจำ
- ใส่จำนวนเงิน (รองรับทศนิยม 2 ตำแหน่ง)
- เลือกสถานะจาก dropdown
- เลือกวิธีการชำระเงิน

### 2. การติดตาม CID-P
- บันทึกวันที่และจำนวนเงิน CID-P ที่จ่าย
- บันทึกวันที่และจำนวนเงิน CID-P ที่เข้า
- ระบบจะคำนวณผลต่างอัตโนมัติ

### 3. การคืนเงินมัดจำ
- บันทึกวันที่คืนเงิน
- ใส่จำนวนเงินที่คืน
- ระบบจะอัปเดตยอดคงเหลือทันที

### 4. การดูการแจ้งเตือน
- เข้าหน้า Dashboard
- ดูการ์ด "เงินมัดจำ CID ค้างชำระ"
- คลิก "ดูรายละเอียด" เพื่อดูรายชื่อคนงาน
- คลิก "Export Excel" เพื่อส่งออกรายงาน

## ❓ FAQ และ Troubleshooting

### Q: แท็บการเงินไม่แสดงในฟอร์ม?
A: ตรวจสอบว่าผู้ใช้มีสิทธิ์ `account-update-labour` หรือไม่

### Q: การคำนวณยอดคงเหลือผิดพลาด?
A: ตรวจสอบว่าใส่ตัวเลขถูกต้องและไม่มีอักขระพิเศษ

### Q: การแจ้งเตือนไม่ทำงาน?
A: ตรวจสอบว่ามีการบันทึก `labour_cid_stand_date` (วันที่ยื่น CID) หรือไม่

### Q: ไม่สามารถส่งออก Excel ได้?
A: ตรวจสอบ Laravel Excel package และการตั้งค่า permissions ของ storage directory
